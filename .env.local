# API Mode Configuration
# Mode: local (Prisma) | external (Backend API)
NEXT_PUBLIC_API_MODE=external

# Backend API URL (used when mode is external)
# ใช้ Next.js API proxy เพื่อหลีกเลี่ยงปัญหา CORS
NEXT_PUBLIC_API_URL=/api

# Go Backend URL (ใช้โดย Next.js API proxy)
GO_BACKEND_URL=http://localhost:8080

# Maintenance Mode (true = show maintenance page, false = normal)
MAINTENANCE_MODE=false
