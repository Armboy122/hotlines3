// Planning Calendar is a read-only projection view (team plan + monthly plan merged).
// No create/update/delete mutations are needed — calendar data is derived.
// Future mutations for calendar overrides can be added here when the backend supports them.

export {} // ensure file is a module for re-export
